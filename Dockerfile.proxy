FROM node:18-alpine

# Set working directory
WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (proxy needs dev dependencies like express, cors)
RUN npm ci

# Copy proxy source
COPY netlify/functions/proxy.js ./proxy.js

# Expose port
EXPOSE 3001

# Start the proxy server
CMD ["node", "proxy.js"]