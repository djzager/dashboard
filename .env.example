# Client-side environment variables (prefixed with VITE_)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
VITE_PROXY_URL=http://localhost:3001
VITE_LATITUDE=38.4707
VITE_LONGITUDE=-78.0169
VITE_USE_MOCK_DATA=false

# Server-side environment variables (for local proxy server and Netlify Functions)
FD_TOKEN=your_firstdue_bearer_token_here
PROXY_PORT=3001

# For Netlify deployment:
# 1. Set FD_TOKEN in Netlify dashboard under Environment Variables
# 2. VITE_ variables are automatically included in the build
# 3. PROXY_PORT is not needed for Netlify Functions
# 4. Weather data uses Open-Meteo (free, no API key required)